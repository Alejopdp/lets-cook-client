import React from "react";
import LoginForm from "../components/loginForm";

const Login = (props) => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Login;
